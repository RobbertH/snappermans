    name: wallclaimer
    version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
    summary: claim your friends' backgrounds # 79 char long summary
    description: |
        WallClaimer enables wallpaper modificiation of anyone you connect with.
    grade: devel # must be 'stable' to release into candidate/stable channels
    confinement: devmode # use 'strict' once you have the right plugs and slots
    parts:
        my-part: # See 'snapcraft plugins'
            plugin: nil
    
    apps:
        wallclaimer:
            command: wallclaimer
